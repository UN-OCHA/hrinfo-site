Put the module in a discoverable modules directory and enable it via
the admin/modules administration page.

Visit admin/config/development/performance to set an autocomplete text
size limit (the limit is disabled by default).
