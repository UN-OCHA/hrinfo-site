<?php
/**
 * @file
 * Code for the Admin tools feature.
 */

include_once 'hr_admin.features.inc';
/**
 * @file
 * Code for the Admin tools feature.
 */

/**
 * Implements hook_menu().
 */
function hr_admin_menu() {
  $items = array();

  $items['admin/config/hrinfo'] = array(
    'title' => 'Humanitarian Response Admin',
    'description' => 'Admin tools for Humanitarian Response',
    'position' => 'right',
    'weight' => -15,
    'page callback' => 'system_admin_menu_block_page',
    'access arguments' => array('access administration pages'),
    'file' => 'system.admin.inc',
    'file path' => drupal_get_path('module', 'system'),
  );

  /*$items['admin/config/hrinfo/clean-urls'] = array(
    'title' => 'Clean URLs',
    'description' => 'Enable or disable clean URLs for your site.',
    'page callback' => 'drupal_get_form',
    'page arguments' => array('system_clean_url_settings'),
    'access arguments' => array('administer site configuration'),
    'file' => 'system.admin.inc',
    'weight' => 5,
    'file path' => drupal_get_path('module', 'system'),
  );*/

  return $items;
}


/**
 * Implements hook_menu_alter().
 */
function hr_admin_menu_alter(&$items) {

  if (isset($items['admin/config/hrinfo/content-authors-activity'])) {
    dpm($items['admin/config/hrinfo/content-authors-activity']);
  }
}
